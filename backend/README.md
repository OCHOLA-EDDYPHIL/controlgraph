# ControlGraph Canary backend

The backend is a Python 3.12 package containing:

- a read-only FastAPI scaffold;
- a CLI for local configuration and fence checks; and
- a dependency-free epoch authority primitive.

It performs no Cloud Run operations in this revision.
